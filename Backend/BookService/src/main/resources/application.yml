spring:
  datasource:
    url: jdbc:mysql://localhost:3307/bookstore?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: BookServicePassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true
      file-size-threshold: 2KB
      location: /tmp
        

  application:
    name: BookService

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.porfolio.BookService: DEBUG

server:
  port: 8083
  tomcat:
    max-http-form-post-size: 10MB
      

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:dbcx8aofx}
  api-key: ${CLOUDINARY_API_KEY:331892227717248}
  api-secret: ${CLOUDINARY_API_SECRET:VlE5hF0Zs6Q3rByHSMV7K7bjQ8M}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka