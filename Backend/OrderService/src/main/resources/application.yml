spring:
  datasource:
    url: jdbc:mysql://localhost:3307/orders?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: LibreriaOnlinePassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
        

  application:
    name: BookService

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.porfolio.BookService: DEBUG

server:
  port: 8084
      



eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka